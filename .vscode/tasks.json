{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Structurizr Lite Up",
            "type": "shell",
            "command": "docker run -it --rm -p 9000:8080 -v ${workspaceFolder}/diagrams:/usr/local/structurizr structurizr/lite"
        }
    ]
}